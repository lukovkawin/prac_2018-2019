from setuptools import setup

setup(name='Nash_equilibrium',
      version='0.1',
      description='Nash_equilibrium',
      license='MIT',
      packages=['Nash_equilibrium'],
      test_suite='nose.collector',
      tests_require=['nose'],
      zip_safe=False)